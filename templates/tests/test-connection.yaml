apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "storage-migration.fullname" . }}-test-connection"
  labels:
    {{- include "storage-migration.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "storage-migration.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
